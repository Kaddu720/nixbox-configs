diff --git a/dwm.c b/dwm.c
index f1d86b2..7543d2d 100644
--- a/dwm.c
+++ b/dwm.c
@@ -1426,7 +1426,6 @@ sendmon(Client *c, Monitor *m)
 	detach(c);
 	detachstack(c);
 	c->mon = m;
-	c->tags = m->tagset[m->seltags]; /* assign tags of target monitor */
 	attach(c);
 	attachstack(c);
 	focus(NULL);
